class Publication {
    constructor(id, title, description, autor, image, fecha) {
      this.id = id;
      this.titulo = title;
      this.descripcion = description;
      this.autor = autor;
      this.image = image;
      this.fecha_publicacion = fecha;
    }
  }
  
  export default Publication;